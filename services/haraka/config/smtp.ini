; SMTP Server Configuration
port=2525
host=0.0.0.0

; Connection settings
max_connections=1000
max_received_count=100

; Enable TLS (optional for dev)
; key=/path/to/key.pem
; cert=/path/to/cert.pem

; Timeouts (in seconds)
inactivity_timeout=300
